#include <cstdio>
#include <map>

int main(int argc, char *argv[])
{
    int linenum, i;
    char linecon[100];

    scanf("%d", &linenum);
    for (i = 0; i < linenum; ++i) {
        fgets(linecon, 100, stdin);
        for (char* c=linecon; )
    }
    return 0;
}
